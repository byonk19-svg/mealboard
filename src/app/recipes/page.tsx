import { PlaceholderPage } from "@/components/shared/placeholder-page";

export default function RecipesPage() {
  return (
    <PlaceholderPage
      title="Recipes"
      description="A future recipe library for saved meals, structured ingredients, statuses, tags, and profile approvals."
    />
  );
}
